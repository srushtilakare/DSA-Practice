#include<iostream>
using namespace std;

void swap(int *a, int *b) {
  int temp = *a;
  *a = *b;
  *b = temp;
}

int main() {
  int x, y;
  cout<<"\nEnter 1st Number : ";
  cin>>x;
  cout<<"\nEnter 2nd Number : ";
  cin>>y;
  
  swap(&x, &y);
  
  return 0;
}
